--[[
  REST API

  No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

  The version of the OpenAPI document: v1
  Contact: support@apibricks.io
  Generated by: https://openapi-generator.tech
]]

--package openapiclient

local http_request = require "http.request"
local http_util = require "http.util"
local dkjson = require "dkjson"
local basexx = require "basexx"

-- model import
local openapiclient_v1_exchange_rate = require "openapiclient.model.v1_exchange_rate"
local openapiclient_v1_exchange_rates = require "openapiclient.model.v1_exchange_rates"
local openapiclient_v1_exchange_rates_timeseries_item = require "openapiclient.model.v1_exchange_rates_timeseries_item"
local openapiclient_v1_timeseries_period = require "openapiclient.model.v1_timeseries_period"

local exchange_rates_api = {}
local exchange_rates_api_mt = {
	__name = "exchange_rates_api";
	__index = exchange_rates_api;
}

local function new_exchange_rates_api(authority, basePath, schemes)
	local schemes_map = {}
	for _,v in ipairs(schemes) do
		schemes_map[v] = v
	end
	local default_scheme = schemes_map.https or schemes_map.http
	local host, port = http_util.split_authority(authority, default_scheme)
	return setmetatable({
		host = host;
		port = port;
		basePath = basePath or "https://rest.coinapi.io";
		schemes = schemes_map;
		default_scheme = default_scheme;
		http_username = nil;
		http_password = nil;
		api_key = {};
		access_token = nil;
	}, exchange_rates_api_mt)
end

function exchange_rates_api:get_specific_rate(asset_id_base, asset_id_quote, time)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/exchangerate/%s/%s?time=%s",
			self.basePath, asset_id_base, asset_id_quote, http_util.encodeURIComponent(time));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")

	-- api key in headers 'X-CoinAPI-Key'
	if self.api_key['X-CoinAPI-Key'] then
		req.headers:upsert("ApiKey", self.api_key['X-CoinAPI-Key'])
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_v1_exchange_rate.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function exchange_rates_api:v1_exchangerate_asset_id_base_asset_id_quote_history_get(asset_id_base, asset_id_quote, period_id, time_start, time_end, limit)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/exchangerate/%s/%s/history?period_id=%s&time_start=%s&time_end=%s&limit=%s",
			self.basePath, asset_id_base, asset_id_quote, http_util.encodeURIComponent(period_id), http_util.encodeURIComponent(time_start), http_util.encodeURIComponent(time_end), http_util.encodeURIComponent(limit));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")

	-- api key in headers 'X-CoinAPI-Key'
	if self.api_key['X-CoinAPI-Key'] then
		req.headers:upsert("ApiKey", self.api_key['X-CoinAPI-Key'])
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_v1_exchange_rates_timeseries_item.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function exchange_rates_api:v1_exchangerate_asset_id_base_get(asset_id_base, filter_asset_id, invert, time)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/exchangerate/%s?filter_asset_id=%s&invert=%s&time=%s",
			self.basePath, asset_id_base, http_util.encodeURIComponent(filter_asset_id), http_util.encodeURIComponent(invert), http_util.encodeURIComponent(time));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")

	-- api key in headers 'X-CoinAPI-Key'
	if self.api_key['X-CoinAPI-Key'] then
		req.headers:upsert("ApiKey", self.api_key['X-CoinAPI-Key'])
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		return openapiclient_v1_exchange_rates.cast(result), headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function exchange_rates_api:v1_exchangerate_history_periods_get()
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/exchangerate/history/periods",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "text/plain", "application/json", "text/json", "application/x-msgpack" }
	req.headers:upsert("content-type", "text/plain")

	-- api key in headers 'X-CoinAPI-Key'
	if self.api_key['X-CoinAPI-Key'] then
		req.headers:upsert("ApiKey", self.api_key['X-CoinAPI-Key'])
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_v1_timeseries_period.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

return {
	new = new_exchange_rates_api;
}
