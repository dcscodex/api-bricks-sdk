--[[
  REST API

  No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

  The version of the OpenAPI document: v1
  Contact: support@apibricks.io
  Generated by: https://openapi-generator.tech
]]

--package openapiclient

local http_request = require "http.request"
local http_util = require "http.util"
local dkjson = require "dkjson"
local basexx = require "basexx"

-- model import
local openapiclient_dto_filing_metadata_dto = require "openapiclient.model.dto_filing_metadata_dto"
local openapiclient_dto_filing_sort_by = require "openapiclient.model.dto_filing_sort_by"
local openapiclient_mvc_problem_details = require "openapiclient.model.mvc_problem_details"
local openapiclient_mvc_validation_problem_details = require "openapiclient.model.mvc_validation_problem_details"

local filing_metadata_api = {}
local filing_metadata_api_mt = {
	__name = "filing_metadata_api";
	__index = filing_metadata_api;
}

local function new_filing_metadata_api(authority, basePath, schemes)
	local schemes_map = {}
	for _,v in ipairs(schemes) do
		schemes_map[v] = v
	end
	local default_scheme = schemes_map.https or schemes_map.http
	local host, port = http_util.split_authority(authority, default_scheme)
	return setmetatable({
		host = host;
		port = port;
		basePath = basePath or "https://api.sec.finfeedapi.com";
		schemes = schemes_map;
		default_scheme = default_scheme;
		http_username = nil;
		http_password = nil;
		api_key = {};
		access_token = nil;
	}, filing_metadata_api_mt)
end

function filing_metadata_api:v1_filings_get(cik, ticker, form_type, filling_date_start, filling_date_end, report_date_start, report_date_end, items_contain, page_size, page_number, sort_by, sort_order)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		port = self.port;
		path = string.format("%s/v1/filings?cik=%s&ticker=%s&form_type=%s&filling_date_start=%s&filling_date_end=%s&report_date_start=%s&report_date_end=%s&items_contain=%s&page_size=%s&page_number=%s&sort_by=%s&sort_order=%s",
			self.basePath, http_util.encodeURIComponent(cik), http_util.encodeURIComponent(ticker), http_util.encodeURIComponent(form_type), http_util.encodeURIComponent(filling_date_start), http_util.encodeURIComponent(filling_date_end), http_util.encodeURIComponent(report_date_start), http_util.encodeURIComponent(report_date_end), http_util.encodeURIComponent(items_contain), http_util.encodeURIComponent(page_size), http_util.encodeURIComponent(page_number), http_util.encodeURIComponent(sort_by), http_util.encodeURIComponent(sort_order));
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")


	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		local body, err, errno2 = stream:get_body_as_string()
		-- exception when getting the HTTP body
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		local result, _, err3 = dkjson.decode(body)
		-- exception when decoding the HTTP body
		if result == nil then
			return nil, err3
		end
		for _, ob in ipairs(result) do
			openapiclient_dto_filing_metadata_dto.cast(ob)
		end
		return result, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

return {
	new = new_filing_metadata_api;
}
