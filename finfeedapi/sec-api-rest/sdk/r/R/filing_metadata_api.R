#' REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Contact: support@apibricks.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title FilingMetadata operations
#' @description FilingMetadataApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  V1FilingsGet  ####################
#'
#' library(openapi)
#' var_cik <- 56 # integer | Filter by Central Index Key (CIK) (Optional)
#' var_ticker <- "ticker_example" # character | Filter by stock ticker symbol (Optional)
#' var_form_type <- "form_type_example" # character | Filter by form type(s) (e.g., \"10-K\", \"8-K\"). Multiple values can be comma-separated (Optional)
#' var_filling_date_start <- "filling_date_start_example" # character | Filter by filling date start (inclusive), format YYYY-MM-DD (Optional)
#' var_filling_date_end <- "filling_date_end_example" # character | Filter by filling date end (inclusive), format YYYY-MM-DD (Optional)
#' var_report_date_start <- "report_date_start_example" # character | Filter by report date start (inclusive), format YYYY-MM-DD (Optional)
#' var_report_date_end <- "report_date_end_example" # character | Filter by report date end (inclusive), format YYYY-MM-DD (Optional)
#' var_items_contain <- "items_contain_example" # character | Filter filings where the 'Items' field contains the specified text (Optional)
#' var_page_size <- 56 # integer | Number of results per page (default: 50, max: 200) (Optional)
#' var_page_number <- 56 # integer | Page number to retrieve (default: 1) (Optional)
#' var_sort_by <- DTO.FilingSortBy$new() # DTOFilingSortBy | Field to sort results by (default: AccessionNumber) (Optional)
#' var_sort_order <- "desc" # character | Sort order (asc or desc, default: desc) (Optional)
#'
#' #Query SEC filing metadata
#' api_instance <- FilingMetadataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1FilingsGet(cik = var_cik, ticker = var_ticker, form_type = var_form_type, filling_date_start = var_filling_date_start, filling_date_end = var_filling_date_end, report_date_start = var_report_date_start, report_date_end = var_report_date_end, items_contain = var_items_contain, page_size = var_page_size, page_number = var_page_number, sort_by = var_sort_by, sort_order = var_sort_orderdata_file = "result.txt")
#' result <- api_instance$V1FilingsGet(cik = var_cik, ticker = var_ticker, form_type = var_form_type, filling_date_start = var_filling_date_start, filling_date_end = var_filling_date_end, report_date_start = var_report_date_start, report_date_end = var_report_date_end, items_contain = var_items_contain, page_size = var_page_size, page_number = var_page_number, sort_by = var_sort_by, sort_order = var_sort_order)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FilingMetadataApi <- R6::R6Class(
  "FilingMetadataApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new FilingMetadataApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Query SEC filing metadata
    #'
    #' @param cik (optional) Filter by Central Index Key (CIK)
    #' @param ticker (optional) Filter by stock ticker symbol
    #' @param form_type (optional) Filter by form type(s) (e.g., \"10-K\", \"8-K\"). Multiple values can be comma-separated
    #' @param filling_date_start (optional) Filter by filling date start (inclusive), format YYYY-MM-DD
    #' @param filling_date_end (optional) Filter by filling date end (inclusive), format YYYY-MM-DD
    #' @param report_date_start (optional) Filter by report date start (inclusive), format YYYY-MM-DD
    #' @param report_date_end (optional) Filter by report date end (inclusive), format YYYY-MM-DD
    #' @param items_contain (optional) Filter filings where the 'Items' field contains the specified text
    #' @param page_size (optional) Number of results per page (default: 50, max: 200)
    #' @param page_number (optional) Page number to retrieve (default: 1)
    #' @param sort_by (optional) Field to sort results by (default: AccessionNumber)
    #' @param sort_order (optional) Sort order (asc or desc, default: desc) (default value: "desc")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[DTOFilingMetadataDto]
    V1FilingsGet = function(cik = NULL, ticker = NULL, form_type = NULL, filling_date_start = NULL, filling_date_end = NULL, report_date_start = NULL, report_date_end = NULL, items_contain = NULL, page_size = NULL, page_number = NULL, sort_by = NULL, sort_order = "desc", data_file = NULL, ...) {
      local_var_response <- self$V1FilingsGetWithHttpInfo(cik, ticker, form_type, filling_date_start, filling_date_end, report_date_start, report_date_end, items_contain, page_size, page_number, sort_by, sort_order, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Query SEC filing metadata
    #'
    #' @param cik (optional) Filter by Central Index Key (CIK)
    #' @param ticker (optional) Filter by stock ticker symbol
    #' @param form_type (optional) Filter by form type(s) (e.g., \"10-K\", \"8-K\"). Multiple values can be comma-separated
    #' @param filling_date_start (optional) Filter by filling date start (inclusive), format YYYY-MM-DD
    #' @param filling_date_end (optional) Filter by filling date end (inclusive), format YYYY-MM-DD
    #' @param report_date_start (optional) Filter by report date start (inclusive), format YYYY-MM-DD
    #' @param report_date_end (optional) Filter by report date end (inclusive), format YYYY-MM-DD
    #' @param items_contain (optional) Filter filings where the 'Items' field contains the specified text
    #' @param page_size (optional) Number of results per page (default: 50, max: 200)
    #' @param page_number (optional) Page number to retrieve (default: 1)
    #' @param sort_by (optional) Field to sort results by (default: AccessionNumber)
    #' @param sort_order (optional) Sort order (asc or desc, default: desc) (default value: "desc")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[DTOFilingMetadataDto]) with additional information such as HTTP status code, headers
    V1FilingsGetWithHttpInfo = function(cik = NULL, ticker = NULL, form_type = NULL, filling_date_start = NULL, filling_date_end = NULL, report_date_start = NULL, report_date_end = NULL, items_contain = NULL, page_size = NULL, page_number = NULL, sort_by = NULL, sort_order = "desc", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      if (!str_detect(`filling_date_start`, "^\\d{4}-\\d{2}-\\d{2}$")) {
        stop("Invalid value for `filling_date_start` when calling FilingMetadataApi$V1FilingsGet, must conform to the pattern ^\\d{4}-\\d{2}-\\d{2}$.")
      }

      if (!str_detect(`filling_date_end`, "^\\d{4}-\\d{2}-\\d{2}$")) {
        stop("Invalid value for `filling_date_end` when calling FilingMetadataApi$V1FilingsGet, must conform to the pattern ^\\d{4}-\\d{2}-\\d{2}$.")
      }

      if (!str_detect(`report_date_start`, "^\\d{4}-\\d{2}-\\d{2}$")) {
        stop("Invalid value for `report_date_start` when calling FilingMetadataApi$V1FilingsGet, must conform to the pattern ^\\d{4}-\\d{2}-\\d{2}$.")
      }

      if (!str_detect(`report_date_end`, "^\\d{4}-\\d{2}-\\d{2}$")) {
        stop("Invalid value for `report_date_end` when calling FilingMetadataApi$V1FilingsGet, must conform to the pattern ^\\d{4}-\\d{2}-\\d{2}$.")
      }


      if (`page_size` > 200) {
        stop("Invalid value for `page_size` when calling FilingMetadataApi$V1FilingsGet, must be smaller than or equal to 200.")
      }
      if (`page_size` < 1) {
        stop("Invalid value for `page_size` when calling FilingMetadataApi$V1FilingsGet, must be bigger than or equal to 1.")
      }

      if (`page_number` > 2147483647) {
        stop("Invalid value for `page_number` when calling FilingMetadataApi$V1FilingsGet, must be smaller than or equal to 2147483647.")
      }
      if (`page_number` < 1) {
        stop("Invalid value for `page_number` when calling FilingMetadataApi$V1FilingsGet, must be bigger than or equal to 1.")
      }


      if (!str_detect(`sort_order`, "^(asc|desc)$")) {
        stop("Invalid value for `sort_order` when calling FilingMetadataApi$V1FilingsGet, must conform to the pattern ^(asc|desc)$.")
      }

      query_params[["cik"]] <- `cik`

      query_params[["ticker"]] <- `ticker`

      query_params[["form_type"]] <- `form_type`

      query_params[["filling_date_start"]] <- `filling_date_start`

      query_params[["filling_date_end"]] <- `filling_date_end`

      query_params[["report_date_start"]] <- `report_date_start`

      query_params[["report_date_end"]] <- `report_date_end`

      query_params[["items_contain"]] <- `items_contain`

      query_params[["page_size"]] <- `page_size`

      query_params[["page_number"]] <- `page_number`

      query_params[["sort_by"]] <- `sort_by`

      query_params[["sort_order"]] <- `sort_order`

      local_var_url_path <- "/v1/filings"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[DTOFilingMetadataDto]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
