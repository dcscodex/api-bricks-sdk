#' FinFeedAPI SEC REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Contact: support@apibricks.io
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title FullTextSearch operations
#' @description FullTextSearchApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  V1FullTextGet  ####################
#'
#' library(openapi)
#' var_form_type <- "form_type_example" # character | Filter by form type (e.g., \"10-K\", \"8-K\"). Multiple values can be comma-separated (Optional)
#' var_filling_date_start <- "filling_date_start_example" # character | Filter by filling date start (inclusive), format YYYY-MM-DD (Optional)
#' var_filling_date_end <- "filling_date_end_example" # character | Filter by filling date end (inclusive), format YYYY-MM-DD (Optional)
#' var_text_contains <- "text_contains_example" # character | Keywords that the text must contain. Multiple values can be comma-separated (Optional)
#' var_text_not_contain <- "text_not_contain_example" # character | Keywords that the text must not contain. Multiple values can be comma-separated (Optional)
#' var_page_size <- 56 # integer | Number of results per page (default: 100) (Optional)
#' var_page_number <- 56 # integer | Page number to retrieve (default: 1) (Optional)
#' var_sort_by <- "AccessionNumber" # character | Field to sort by (default: AccessionNumber) (Optional)
#' var_sort_order <- "asc" # character | Sort order (asc or desc). Defaults to asc (Optional)
#'
#' #Full-text search of SEC filing documents
#' api_instance <- FullTextSearchApi$new()
#'
#' # Configure API key authorization: APIKey
#' api_instance$api_client$api_keys["Authorization"] <- Sys.getenv("API_KEY")
#'
#' # Configure HTTP bearer authorization: JWT
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$V1FullTextGet(form_type = var_form_type, filling_date_start = var_filling_date_start, filling_date_end = var_filling_date_end, text_contains = var_text_contains, text_not_contain = var_text_not_contain, page_size = var_page_size, page_number = var_page_number, sort_by = var_sort_by, sort_order = var_sort_orderdata_file = "result.txt")
#' result <- api_instance$V1FullTextGet(form_type = var_form_type, filling_date_start = var_filling_date_start, filling_date_end = var_filling_date_end, text_contains = var_text_contains, text_not_contain = var_text_not_contain, page_size = var_page_size, page_number = var_page_number, sort_by = var_sort_by, sort_order = var_sort_order)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FullTextSearchApi <- R6::R6Class(
  "FullTextSearchApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new FullTextSearchApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Full-text search of SEC filing documents
    #'
    #' @param form_type (optional) Filter by form type (e.g., \"10-K\", \"8-K\"). Multiple values can be comma-separated
    #' @param filling_date_start (optional) Filter by filling date start (inclusive), format YYYY-MM-DD
    #' @param filling_date_end (optional) Filter by filling date end (inclusive), format YYYY-MM-DD
    #' @param text_contains (optional) Keywords that the text must contain. Multiple values can be comma-separated
    #' @param text_not_contain (optional) Keywords that the text must not contain. Multiple values can be comma-separated
    #' @param page_size (optional) Number of results per page (default: 100)
    #' @param page_number (optional) Page number to retrieve (default: 1)
    #' @param sort_by (optional) Field to sort by (default: AccessionNumber) (default value: "AccessionNumber")
    #' @param sort_order (optional) Sort order (asc or desc). Defaults to asc (default value: "asc")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[DTOSecFilingResultDto]
    V1FullTextGet = function(form_type = NULL, filling_date_start = NULL, filling_date_end = NULL, text_contains = NULL, text_not_contain = NULL, page_size = NULL, page_number = NULL, sort_by = "AccessionNumber", sort_order = "asc", data_file = NULL, ...) {
      local_var_response <- self$V1FullTextGetWithHttpInfo(form_type, filling_date_start, filling_date_end, text_contains, text_not_contain, page_size, page_number, sort_by, sort_order, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Full-text search of SEC filing documents
    #'
    #' @param form_type (optional) Filter by form type (e.g., \"10-K\", \"8-K\"). Multiple values can be comma-separated
    #' @param filling_date_start (optional) Filter by filling date start (inclusive), format YYYY-MM-DD
    #' @param filling_date_end (optional) Filter by filling date end (inclusive), format YYYY-MM-DD
    #' @param text_contains (optional) Keywords that the text must contain. Multiple values can be comma-separated
    #' @param text_not_contain (optional) Keywords that the text must not contain. Multiple values can be comma-separated
    #' @param page_size (optional) Number of results per page (default: 100)
    #' @param page_number (optional) Page number to retrieve (default: 1)
    #' @param sort_by (optional) Field to sort by (default: AccessionNumber) (default value: "AccessionNumber")
    #' @param sort_order (optional) Sort order (asc or desc). Defaults to asc (default value: "asc")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[DTOSecFilingResultDto]) with additional information such as HTTP status code, headers
    V1FullTextGetWithHttpInfo = function(form_type = NULL, filling_date_start = NULL, filling_date_end = NULL, text_contains = NULL, text_not_contain = NULL, page_size = NULL, page_number = NULL, sort_by = "AccessionNumber", sort_order = "asc", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      if (!str_detect(`filling_date_start`, "^\\d{4}-\\d{2}-\\d{2}$")) {
        stop("Invalid value for `filling_date_start` when calling FullTextSearchApi$V1FullTextGet, must conform to the pattern ^\\d{4}-\\d{2}-\\d{2}$.")
      }

      if (!str_detect(`filling_date_end`, "^\\d{4}-\\d{2}-\\d{2}$")) {
        stop("Invalid value for `filling_date_end` when calling FullTextSearchApi$V1FullTextGet, must conform to the pattern ^\\d{4}-\\d{2}-\\d{2}$.")
      }





      if (!str_detect(`sort_by`, "^(AccessionNumber|FormType|FilingDate|CompanyName|CIK|DocumentFilename|DocumentDescription)$")) {
        stop("Invalid value for `sort_by` when calling FullTextSearchApi$V1FullTextGet, must conform to the pattern ^(AccessionNumber|FormType|FilingDate|CompanyName|CIK|DocumentFilename|DocumentDescription)$.")
      }

      if (!str_detect(`sort_order`, "^(asc|desc)$")) {
        stop("Invalid value for `sort_order` when calling FullTextSearchApi$V1FullTextGet, must conform to the pattern ^(asc|desc)$.")
      }

      query_params[["form_type"]] <- `form_type`

      query_params[["filling_date_start"]] <- `filling_date_start`

      query_params[["filling_date_end"]] <- `filling_date_end`

      query_params[["text_contains"]] <- `text_contains`

      query_params[["text_not_contain"]] <- `text_not_contain`

      query_params[["page_size"]] <- `page_size`

      query_params[["page_number"]] <- `page_number`

      query_params[["sort_by"]] <- `sort_by`

      query_params[["sort_order"]] <- `sort_order`

      local_var_url_path <- "/v1/full-text"
      # API key authentication
      if ("Authorization" %in% names(self$api_client$api_keys) && nchar(self$api_client$api_keys["Authorization"]) > 0) {
        header_params["Authorization"] <- paste(unlist(self$api_client$api_keys["Authorization"]), collapse = "")
      }
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[DTOSecFilingResultDto]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
